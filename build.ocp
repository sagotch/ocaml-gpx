begin library "gpx"
  dirname += ["src"]
  files = ["gpx.ml"]
  requires = ["str" ; "xml-light"]
end

begin program "test"
  dirname += ["test"]
  files = ["test.ml"]
  requires = ["xml-light" ; "gpx"]
end
